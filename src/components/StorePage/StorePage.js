import * as T from "./StorePageStyle"

function StorePage () {
    return (
        <T.container>
            <T.main>
                <T.upgradeList>
                    <T.upgradeButton></T.upgradeButton>
                </T.upgradeList>
            </T.main>
        </T.container>
    );
}

export default StorePage;